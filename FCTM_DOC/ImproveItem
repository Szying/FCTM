1. 同步行程與紀錄有問題，手機sqlite db 與 server上的db的同步機制需重新設計；
2. 網路同步後，畫面UI不會更新，要切換TAB才會更新，一直按右上角refresh，會一直新增(如上同步有問題)
3. Server用servlet，response 擷取字串(以`符號區隔欄位)，暫無問題，但可改JSON格式；
4. 程式架構有點混亂，手機內部資料儲存方式不一；
5. 英文語系破版
